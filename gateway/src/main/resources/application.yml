server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOrigins: "http://localhost:4200"
                allowedHeaders: "*"
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - DELETE
                  - OPTIONS
                allowCredentials: true
          routes:
            - id: ms-producto
              uri: http://localhost:8081
              predicates:
                - Path=/api/productos/**
            - id: ms-pago
              uri: http://localhost:8082
              predicates:
                - Path=/api/pagos/**,/api/descuentos/**
            - id: ms-pedido
              uri: http://localhost:8083
              predicates:
                - Path=/api/pedidos/**

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.web: DEBUG